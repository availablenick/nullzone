%div.localization.box-shadow
  = link_to 'Início', sections_path
  >
  %b Usuários

%h2.text-white.mb-4
  %b Lista de usuários

- if @users.any?
  %div.row
    %div.user-list-filler-header.col-12

    - @users.each do |user|
      %div.user-cell.col-12.col-sm-6.col-md-4.col-lg-3
        %div.d-flex.flex-column.align-items-center.p-2.text-white
          %div.top.mb-2
            = set_avatar(user, size: 50)

          %div
            %b
              = link_to user.login, user_path(user), class: 'link-color-white'

- else
  %p.text-center.text-white.h4
    Nenhum usuário cadastrado
