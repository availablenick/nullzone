%h1.color-white Painel

%div.panel-grid.flex-row.wrap-items
  - @users.each do |user|
    - if !user.ban
      %div.border-box
        %div.panel-cell.border-box.fill-parent-height.fill-parent-width.flex-row.h-middle-space.wrap-items
          %div.left.flex-row.v-center
            = set_avatar user, size: 29
            %b
              = user.login

          %div.right.flex-row.v-center
            = link_to new_user_ban_path(user) do
              %i.fas.fa-user-lock
              Banir

%hr/

%h1{ style: 'color: white' } Usuários banidos
- if @bans.any?
  - @bans.each do |ban|
    %div.ban-cell.color-white.flex-row.h-middle-space.v-center.text-center
      %span
        = ban.user.login
        = '- Motivo:'
        = "#{ban.reason}."

        - if ban.permanent?
          Banido permanentemente.
        - else
          Banido até
          = succeed '.' do
            = format_date(ban.expires_at)

      %div
        - an_class = 'clear-border font-normal hover-white link-like'
        = link_to edit_ban_path(ban), class: an_class do
          %i.fas.fa-edit
          Alterar ban

        = button_to ban_path(ban), class: an_class, form_class: 'inline', method: :delete do
          %i.fas.fa-laugh
          Desbanir

- else
  Não há usuários banidos.
